import React from 'react';

const MyEnrolled = () => {
    const enrolledCourses = useLoaderData();

    return (
        <div>
            MyEnrolled
        </div>
    );
};

export default MyEnrolled;