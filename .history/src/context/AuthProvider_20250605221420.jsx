import React from 'react';
import { AuthContext } from './AuthContext';

const AuthProvider = () => {
    return (
        <AuthContext>
            
        </AuthContext>
    );
};

export default AuthProvider;